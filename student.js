class Student {
    constructor(id, firstName, lastName, grade) {

            this.id = id;
            this.firstName = firstName;
            this.lastName = lastName;
            this.grade = grade;
           

    }  
    
}